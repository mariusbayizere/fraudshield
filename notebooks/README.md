# notebooks

Analysis and figures only (build prompt H.3): SHAP analysis, model comparison, calibration and
fairness. Delivered in **M4**. Notebooks import logic from `fraudshield_ml`, run top-to-bottom in
CI, and are committed with outputs cleared.
