"""Python message classes compiled from `contracts/proto`, checked in and never edited by hand.

Regenerate with `python -m fraudshield_ml.serving.generated.regenerate`; a test fails when the
checked-in files differ from what the frozen contract compiles to. Only the messages are
generated: the service is registered by hand in `serving.server` with typed handlers, so there is
no untyped `_pb2_grpc` module to carry.
"""
