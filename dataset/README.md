# dataset

FraudShield-EAC-Transactions synthetic data generator (build prompt E.3). Delivered in **M2**.

Planned contents: `generator/` (seeded agent-based simulator), `realism_checks/` (single-feature
AUC limits, label noise, novelty-variant placement), `datasheet.md` (Gebru et al.). Generated
data is never committed (`.gitignore`); it is published by the author with checksums.
