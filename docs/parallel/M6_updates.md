# M6 — parallel-branch updates (`m6/decision`)

Written by the M6 session instead of `requirements.yaml`, `docs/backlog/`, `lab_notebook.md` and
`SESSION_STATE.md`, which this branch does not edit. Whoever merges M6 folds these entries into
those files. Newest entries at the bottom of each section.

## Numbering reserved by this branch

Other milestones are being built in parallel, and ADR and Flyway numbers are global.

| Kind | Reserved for M6 | Why |
|---|---|---|
| ADR | `0060`–`0069` | `0027` and `0028` already exist on `m4/generalisation` and `m5/scoring` |
| Flyway | `V60`–`V69` | M7 will add identity migrations; `V12`… are left to it |

## Files outside the M6 modules that this branch touches

M6 owns `backend/{ingest,decision,rules,notify}`. These edits outside them were unavoidable:

| File | Change | Why |
|---|---|---|
| `backend/pom.xml` | adds the four modules and their managed versions | a module must be listed by its parent |
| `docs/traceability/requirements_matrix.md` | regenerated by `fs-traceability render` | generated from test tags; the governance hook fails when stale. `requirements.yaml` is not edited |
| `docs/parallel/M6_updates.md` | this file | |

## Traceability updates to apply at merge

Rows to move once M6 is reviewed. Evidence is the named test, run on this branch.

| Row | Proposed status | Evidence |
|---|---|---|
| FR-05-05 (rule DSL half) | IN_PROGRESS → the DSL, compiler and null semantics are done; the rule-builder endpoints are staff API (M7/M8) | `RuleCompilerTest`, `RuleDraftVectorsTest`, `TruthTest`, `FieldCatalogueDriftTest` |
