# design-tokens

Single source for colour (including the D-33 text-safe tokens), typography, spacing, radii,
elevation, motion and breakpoints (D-36), generating the MUI theme, Tailwind configuration and
CSS variables. Delivered in **M8**; the contrast measurement it relies on exists in
`frontend/src/design-system/color/contrast.ts`.
